# docs/

Place your portfolio screenshot here as `preview.png`.

You can take a screenshot with:
- **macOS:** `Cmd+Shift+4`, then drag a region
- **Windows:** `Win+Shift+S`
- **Browser DevTools:** `Ctrl+Shift+P` → "Capture full size screenshot"

Then commit it so the README preview image works on GitHub.
